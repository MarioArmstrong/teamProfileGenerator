const index = require('../index');

describe('', () => {
    it('should return', () => {
        expect();
        expect();
    });
    it('should return', () => {
        expect();
        expect();
    });
    it('should return', () => {
        expect();
        expect();
    });
    it('should return', () => {
        expect();
        expect();
    });
});//END describe